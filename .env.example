# MS Teams AI Agent - Environment Variables Template
# Copy this file to .env and fill in the values

# =============================================================================
# Azure Configuration
# =============================================================================
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_LOCATION=eastus
AZURE_RESOURCE_GROUP=rg-teams-ai-agent

# =============================================================================
# Azure OpenAI Configuration
# =============================================================================
AZURE_OPENAI_ENDPOINT=https://your-openai.openai.azure.com
AZURE_OPENAI_API_KEY=your-api-key
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# =============================================================================
# Bot Framework Configuration
# =============================================================================
BOT_APP_ID=your-bot-app-id
BOT_APP_PASSWORD=your-bot-app-password
BOT_TENANT_ID=your-tenant-id

# =============================================================================
# Application Configuration
# =============================================================================
APP_ENV=development
LOG_LEVEL=INFO
MAX_CONVERSATION_HISTORY=10

# =============================================================================
# MCP Configuration
# =============================================================================
# JSON configuration file path (optional if using environment variables)
MCP_CONFIG_PATH=./mcp_servers.json
MCP_CONNECTION_TIMEOUT=30

# MCP Server Environment Variable Configuration (for Azure Container Apps)
# Configure MCP servers via environment variables instead of JSON file
# Format: MCP_SERVER_N_* where N is the server number (1, 2, 3, etc.)
#
# Example Server 1 - Filesystem:
# MCP_SERVER_1_NAME=filesystem
# MCP_SERVER_1_COMMAND=npx
# MCP_SERVER_1_ARGS=-y,@modelcontextprotocol/server-filesystem,/workspace
# MCP_SERVER_1_TRANSPORT=stdio
# MCP_SERVER_1_ENABLED=true
# MCP_SERVER_1_DESCRIPTION=Filesystem access for project files
#
# Example Server 2 - Web Search:
# MCP_SERVER_2_NAME=web-search
# MCP_SERVER_2_COMMAND=python
# MCP_SERVER_2_ARGS=-m,mcp_server_web_search
# MCP_SERVER_2_TRANSPORT=stdio
# MCP_SERVER_2_ENABLED=true
# MCP_SERVER_2_ENV_API_KEY=your-search-api-key
# MCP_SERVER_2_DESCRIPTION=Web search integration
#
# Optimization hint (optional):
# MCP_SERVER_COUNT=2
#
# Environment variable fields supported:
# - MCP_SERVER_N_NAME: Server name (required)
# - MCP_SERVER_N_COMMAND: Command to execute (required)
# - MCP_SERVER_N_ARGS: Comma-separated arguments (optional)
# - MCP_SERVER_N_TRANSPORT: Transport type - stdio or sse (default: stdio)
# - MCP_SERVER_N_ENABLED: Enable/disable server - true or false (default: true)
# - MCP_SERVER_N_DESCRIPTION: Human-readable description (optional)
# - MCP_SERVER_N_ENV_*: Environment variables for the server (optional)
#
# Note: Environment variables override JSON configuration for same-named servers

# =============================================================================
# Azure Resources (Set by azd during deployment)
# =============================================================================
# AZURE_CONTAINER_REGISTRY_NAME=auto-set-by-azd
# AZURE_CONTAINER_APP_NAME=auto-set-by-azd
# AZURE_KEY_VAULT_NAME=auto-set-by-azd
# APPLICATIONINSIGHTS_CONNECTION_STRING=auto-set-by-azd

# =============================================================================
# Optional: GitHub Integration (for MCP server)
# =============================================================================
# GITHUB_TOKEN=your-github-personal-access-token

# =============================================================================
# Optional: Slack Integration (for MCP server)
# =============================================================================
# SLACK_BOT_TOKEN=your-slack-bot-token
# SLACK_TEAM_ID=your-slack-team-id
